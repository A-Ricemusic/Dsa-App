# Convex deployment values. `bunx convex dev` normally writes these for you.
CONVEX_DEPLOYMENT=dev:your-deployment
CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_SITE_URL=https://your-deployment.convex.site

# WorkOS AuthKit server-session configuration.
WORKOS_CLIENT_ID=client_your_client_id
WORKOS_REDIRECT_URI=http://localhost:5173/callback
WORKOS_API_KEY=sk_test_your_api_key
# Generate once with `openssl rand -base64 32` and keep it stable.
WORKOS_COOKIE_PASSWORD=replace_with_at_least_32_random_characters

# Convex deployments need only WORKOS_CLIENT_ID from this WorkOS block.
